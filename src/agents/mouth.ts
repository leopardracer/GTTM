export const meta = { name: "MOUTH", role: "content operations" };

export async function readout(): Promise<string[]> {
  return [
    "no content pipeline wired up in v0.1 — MOUTH is a placeholder crew slot.",
    "future architecture: automated content generation, X monitoring (see README roadmap).",
  ];
}
